/**
 * Port for sync log persistence.
 *
 * Expected methods:
 * - startSyncLog(context)
 * - appendSyncLogEntry(context)
 * - finishSyncLog(context)
 * - failSyncLog(context)
 */
export const SyncLogPort = Object.freeze({});

