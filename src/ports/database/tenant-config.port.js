/**
 * Port for tenant configuration persistence.
 *
 * Expected methods:
 * - findActiveTenant({ tenantId, tenantKey, portalId })
 * - getTenantModels(tenantKey)
 * - resolveTenantConfig({ tenantId, tenantKey, portalId })
 */
export const TenantConfigPort = Object.freeze({});
