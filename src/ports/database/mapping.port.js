/**
 * Port for field and object mapping persistence.
 *
 * Expected methods:
 * - getMappingsByObjectType(hubspotCredentialId, sourceType, targetType, tenantModels)
 * - ensureDefaultMappings(context)
 */
export const MappingPort = Object.freeze({});

