/**
 * Port for HubSpot contact operations.
 *
 * Expected methods:
 * - searchContact({ token, filters, properties })
 * - createContact({ token, payload })
 * - updateContact({ token, contactId, payload })
 */
export const HubspotContactPort = Object.freeze({});

