/**
 * Port for HubSpot product operations.
 *
 * Expected methods:
 * - searchProduct({ token, filters, properties })
 * - createProduct({ token, payload })
 * - updateProduct({ token, productId, payload })
 * - batchUpsertProducts({ token, payload })
 */
export const HubspotProductPort = Object.freeze({});

