/**
 * Port for HubSpot company operations.
 *
 * Expected methods:
 * - searchCompany({ token, filters, properties })
 * - createCompany({ token, payload })
 * - updateCompany({ token, companyId, payload })
 */
export const HubspotCompanyPort = Object.freeze({});

