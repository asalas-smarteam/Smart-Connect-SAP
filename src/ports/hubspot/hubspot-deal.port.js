/**
 * Port for HubSpot deal operations.
 *
 * Expected methods:
 * - getDealById({ token, dealId, properties })
 * - getDealWithAssociations({ token, dealId, associations })
 * - updateDeal({ token, dealId, payload })
 * - getDealLineItems({ token, dealId })
 */
export const HubspotDealPort = Object.freeze({});

