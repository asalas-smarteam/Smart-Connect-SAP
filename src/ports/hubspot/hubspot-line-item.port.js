/**
 * Port for HubSpot line item operations.
 *
 * Expected methods:
 * - getLineItemById({ token, lineItemId, properties })
 * - updateLineItem({ token, lineItemId, payload })
 * - batchReadLineItems({ token, inputs, properties })
 * - batchUpdateLineItems({ token, payload })
 */
export const HubspotLineItemPort = Object.freeze({});

