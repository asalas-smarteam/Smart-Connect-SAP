/**
 * Port for asynchronous job queues.
 *
 * Expected methods:
 * - addManualJob(payload)
 * - addScheduledJob(payload)
 * - getQueue()
 * - close()
 */
export const JobQueuePort = Object.freeze({});

