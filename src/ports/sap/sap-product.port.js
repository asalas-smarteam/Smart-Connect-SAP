/**
 * Port for SAP item/product operations.
 *
 * Expected methods:
 * - getItemByCode({ sapConfig, itemCode })
 * - getItems({ sapConfig, filters })
 * - getStock({ sapConfig, itemCode, warehouseCode })
 * - getItemWarehouses({ sapConfig, itemCode })
 */
export const SapProductPort = Object.freeze({});

