/**
 * Port for SAP price operations.
 *
 * Expected methods:
 * - getItemPrice({ sapConfig, cardCode, itemCode, date })
 * - getItemPriceByPriceList({ sapConfig, itemCode, priceList })
 * - callItemsServiceGetItemPrice({ sapConfig, payload })
 */
export const SapPricePort = Object.freeze({});

