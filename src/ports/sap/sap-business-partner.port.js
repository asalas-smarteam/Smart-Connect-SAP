/**
 * Port for SAP Business Partner operations.
 *
 * Expected methods:
 * - getBusinessPartnerByCardCode({ sapConfig, cardCode })
 * - searchBusinessPartners({ sapConfig, filters })
 * - createBusinessPartner({ sapConfig, payload })
 * - updateBusinessPartner({ sapConfig, cardCode, payload })
 */
export const SapBusinessPartnerPort = Object.freeze({});

