/**
 * Port for SAP Service Layer session operations.
 *
 * Expected methods:
 * - resolveTenantKey(config)
 * - getSessionCookie(config)
 * - invalidateSession(configOrTenantKey)
 * - isSessionInvalidError(error)
 */
export const SapSessionPort = Object.freeze({});

