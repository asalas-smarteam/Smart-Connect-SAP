/**
 * Port for SAP sales order operations.
 *
 * Expected methods:
 * - createOrder({ sapConfig, payload })
 * - getOrderByDocEntry({ sapConfig, docEntry })
 * - getOrders({ sapConfig, filters })
 * - updateOrder({ sapConfig, docEntry, payload })
 */
export const SapOrderPort = Object.freeze({});

