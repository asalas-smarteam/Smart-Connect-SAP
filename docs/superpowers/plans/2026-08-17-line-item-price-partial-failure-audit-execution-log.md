# SDD ledger — plan: docs/superpowers/plans/2026-08-17-line-item-price-partial-failure-audit.md

Worktree: `.claude/worktrees/line-item-price-partial-failure` (branch `worktree-line-item-price-partial-failure`)
Base commit: dd9b9a7 (== origin/main, 0 ahead / 0 behind)

## Baseline de tests (Task 0) — anotado antes de tocar código

`NODE_OPTIONS=--experimental-vm-modules npx jest`
→ Test Suites: 4 failed, 153 passed, 157 total
→ Tests: 7 failed, 1289 passed, 1296 total

Suites que YA fallan en HEAD:

1. `tests/unit/application/sendMappedItemsToHubspot.test.js` — fuera del alcance de este plan.
2. `tests/unit/serviceLayerService.test.js` — fuera del alcance de este plan.
3. `tests/unit/application/syncLineItemPrices.test.js` — **2 tests, DENTRO del alcance**:
   - `uses configured SAP tax field as HubSpot line item discount for business partner prices`
   - `uses configured SAP tax field as HubSpot line item discount with tenant price list`
   Causa: `resolveTaxRate` está comentado en `SyncLineItemPrices.js:253-257` y `finalDiscount`
   se inicializa en `0`. Los tests esperan `Discount: 15` y reciben `Discount: 0`.
   `resolveTaxRate` (`:90`) quedó como código muerto.
4. `tests/unit/lineItemPriceWebhook.service.test.js` — **1 test, DENTRO del alcance**:
   - `propertyChange SkippedVersion flow › runs the legacy single line item flow when the mode config is the default`
   Causa: defecto del doble de test. `buildTenantModels()` devuelve `findOne` → `{select:{lean}}`
   sin `.sort`, y `handlePropertyChangeEvent` (`:193`) llama `.sort({createdAt:-1})`.
   Es del flujo propertyChange Legacy, NO del flujo de asociación que modifica este plan.

**Regla para implementadores y revisores:** estos 3 fallos son preexistentes. No son regresiones
y no se arreglan en este trabajo salvo instrucción explícita. Cualquier OTRO fallo sí es propio.

### Suite flaky (no es regresión)

`tests/integration/internalTenant.test.js` falla de forma intermitente en la suite completa con
`Exceeded timeout of 5000 ms for a hook` en el `beforeAll` (línea 19: `MongoMemoryServer.create`).
Corrida sola pasa 4/4. Es arranque en frío del mongod en memoria bajo paralelismo, no un
cambio de código. Verificado el 2026-08-17 tras Task 1.

### Decisión del pre-flight scan (2026-08-17)

`resolveTaxRate` comentado en `SyncLineItemPrices.js:253-257` con `finalDiscount = 0`:
**se deja como está**, decisión del usuario. Los 2 tests siguen rojos como baseline conocido.
Los implementadores de T6/T7/T8 deben preservar `finalDiscount = 0` tal cual y NO arreglarlo.
Hipótesis (sin confirmar): fue deliberado, el impuesto pasó a viajar como `hs_tax_rate_group_id`
en `HubspotLineItemPriceClient.js:43` en vez de como `discount`. Pendiente de decidir aparte.

## Progreso

Task 1: complete (commits 7178ea1..9befe0e, review clean — spec ✅, quality Approved)
Task 1: minor (deferred): `dealId`/`cardCode` no pasan por `sanitizeAuditKeys` en syncLog.service.js:295-296. Sin riesgo hoy (ambos escalares), pero es un hueco literal contra la constraint global.
Task 1: minor (deferred): `calls.slice(0, 200)` en syncLog.service.js:284 conserva las PRIMERAS 200 en orden de llegada, sin preferir las fallidas. Una corrida patológica >200 podría descartar justo el fallo que el audit existe para capturar. Etiquetado plan-mandated (viene del brief).
Task 1: minor (deferred): el comentario de syncLog.service.js:253-256 dice "~85" llamadas pero los componentes que lista suman 65. Nit de exactitud del comentario (error mío al escribir el plan).
Task 2: review 1 — spec ✅, quality Needs fixes. 1 Important (el test no verifica que la rama catch se ejecute), 1 Minor.
Task 2: minor (deferred): `persistAudit` en lineItemPriceWebhook.service.js:75-94 no tiene guard `!LineItemPriceWebhookEvent` a diferencia de sus dos llamadores. Inalcanzable hoy porque ambos guardan antes de llamarlo.
Task 2: fix round 1/5 (1 addressed, 0 open — test reforzado con toHaveBeenCalledTimes(2), aserción del 2º updateOne y spy de logger.warn; commits 6259a2a..c1fd576)
Task 2: complete (commits 9befe0e..c1fd576, review clean)
Task 3: complete (commits c1fd576..cb03e96, review clean — spec ✅, quality Approved)
Task 3: minor (deferred): `readLineItems` en lineItemPriceWebhook.shared.js:123-125 esparce `extraProperties` DESPUÉS de las llaves fijas, así que un extra llamado `id`/`itemCode`/`quantity`/`properties` las sobreescribe en silencio. Importa porque T4 le pasa `miscSourceProperty`, que es un valor configurado por tenant en Mongo: si alguien lo configurara como `quantity`, se corrompería la cantidad y con ella el `lineTotal`. Guard sugerido: filtrar los nombres reservados. Puntero llevado al dispatch de T4.
Task 3: minor (deferred): no hay test de los métodos `readLineItems`/`readDealLineItemIds` del cliente (sólo de las funciones compartidas). La delegación es de una línea; hueco chico en la superficie del puerto.
Task 3: CORRECCIÓN del minor de colisión de llaves (surgida en la revisión de T4 con Opus): mi caracterización era INCORRECTA. Si `miscSourceProperty` fuera `quantity`, la sobreescritura es de VALOR IDÉNTICO (ambos lados leen `recordProperties.quantity ?? null`) y no corrompe nada. Para `id`/`itemCode` la misma colisión ya existía en el `resolveLineItems` borrado, y fallaría ruidosamente con 400 vía `validatePayload` (SyncLineItemPrices.js:45-52), no en silencio. La única llave nueva realmente colisionable es `properties`, que hoy nadie consume. NO hace falta guard antes de shippear.
Task 4: review 1 (Opus) — spec ✅, quality Approved, con 2 Important.
Task 4: Important 1 — cambio de precedencia de errores, etiquetado plan-mandated: el plan subió el chequeo `lineItemIds.length === 0` por encima de `resolveCardCode`, así que un deal sin empresa/contacto Y sin líneas ahora guarda `Deal has no associated line items` en vez de `Associated company or contact is required for the deal`. Ningún test fija la precedencia nueva; el implementador editó el fixture (test:480) para que la aserción vieja siguiera pasando, lo que dejó la deriva invisible.
Task 4: DECISIÓN DEL USUARIO (2026-08-17): gana empresa/contacto. Si el deal no tiene empresa ni contacto debe fallar con ese error y NO debe tocar los line items. Se revierte la precedencia: `resolveCardCode` va antes de cualquier trabajo con líneas.
Task 4: Important 2 — el nuevo fatal `Deal has no readable line items` no tiene NINGÚN test. Es el requisito que impide "éxito sin nada valorizado"; sin test, un refactor futuro lo borra en silencio.
Task 4: minor (deferred): `lineItemFailures` no llega a ningún humano hasta que T8 aterrice — el controlador no lo lee, y los contadores del syncLog sólo cuentan las líneas sobrevivientes, así que una línea descartada por 404 no figura ni como procesada ni como fallida y la respuesta HTTP es `ok: true`. T8 debe shippear en la misma release que T4.
Task 4: fix round 1/5 (4 addressed, 0 open — 1a precedencia de `resolveCardCode` restaurada, 1b fixture revertido, 1c test que fija la precedencia, 2 test del fatal `Deal has no readable line items`; commits 4b6d297..27efff2). El implementador original no se pudo retomar (sin transcript), así que el fix lo hizo uno fresco desde el brief + el report file.
Task 4: complete (commits cb03e96..27efff2, review clean)
Task 4: nota — el re-reviewer repitió como out-of-scope la colisión de llaves de `readLineItems` diciendo que podría "corromper en silencio" el `quantity`. Eso YA fue refutado por la revisión con Opus: la sobreescritura es de valor idéntico. Queda superseded, no re-litigar.
Task 5: review 1 — spec ✅ (con un ⚠️), quality Approved. Cableado verificado por el controller: import en composition:5, clave en composition:26, recorder instanciado dentro de execute (SyncLineItemPrices.js:134), nunca en el constructor.
Task 5: Important (plan-mandated, PENDIENTE DE DECISIÓN DEL USUARIO) — `updateProducts()` llama internamente a `findProductBySKU` (endpoint de SEARCH de HubSpot) una vez por itemCode único. El plan envolvió sólo la llamada externa, etiquetada `/crm/v3/objects/products/batch/update`. Si la búsqueda falla, el audit registra `ok:false` con el path del batch update: atribuye un fallo de search al endpoint equivocado. Agravante: el search API de HubSpot es 5/s account-wide y es el punto caliente conocido de 429 en este proyecto, así que es la llamada con más probabilidad de fallar de todo el flujo.
Task 5: minor (deferred): las tres escrituras a HubSpot se graban sin `data`/cuerpo (sólo el `fetchBusinessPartnerPrice` de SAP adjunta `data`). Lo enviado a HubSpot sólo se recupera vía `auditTrail.response_hubspot.*.payload`, no vía `callRecorder.calls[].request`. Importa para quien escriba el ensamblado de T8: "qué se envió" no vive uniformemente en `calls`.
Task 5: minor (deferred): el test de grabación sólo asegura 2 de las 5 llamadas envueltas, nunca ejercita la rama de tenant-price-list (el payload siempre trae cardCode) ni un camino de fallo (`ok:false`) a través del recorder.
Task 5: DECISIÓN DEL USUARIO (2026-08-17): arreglarlo ahora, en T5.
Task 5: fix round 1/5 (4 addressed a/b/c/d, 0 open — search per-SKU con su propia entrada `POST /crm/v3/objects/products/search`, recorder pasado por param con default noop inline sin importar application, wrap externo intacto, test del search fallando; commits 834645c..e12e6c7)
Task 5: minor (deferred) + RULING del controller: a nivel compuesto, un fallo del search produce DOS entradas `ok:false` — la correcta del search y una del `batch/update` que nunca se emitió, porque el error sigue propagando hasta el wrap externo. Es consecuencia directa de mi propia instrucción (c) "ambas entradas deben existir". Ruling: no se abre otra ronda. La misdirección original está resuelta (la entrada correcta con el path real existe); lo que queda es una entrada redundante con etiqueta imprecisa, no una pista falsa. Que la revisión final de la rama decida si vale limpiarla antes del merge.
Task 5: complete (commits 27efff2..e12e6c7, review clean)
Task 6: complete (commits e12e6c7..c6834c6, review clean — spec ✅, quality Approved, 0 Critical/Important, 7 Minor). Verificado por el controller: bloque comentado de `resolveTaxRate` + función muerta intactos, y los 2 tests rojos siguen fallando por la MISMA causa (`Discount: 15` vs `0`), no por otra.
Task 6: minor (deferred): el test de caché prueba una sola ida a SAP pero NO prueba que la 2ª línea se haya valorizado (no asegura nada sobre `updateLineItems`). Un cache hit que devolviera un objeto parcial pasaría el test y escribiría la línea 2 sin `warehouseStockProperties` ni `tax`. ← PUNTERO A T7: la reconciliación depende justo de ese camino.
Task 6: minor (deferred): nada fija el mapeo a 500 del nuevo fatal; el test sólo asegura el string. El 500 vive en el regex del controlador (lineItemPrice.controller.js:4-8) y un cambio ahí podría volverlo 400 en silencio y cortar los reintentos de HubSpot.
Task 6: minor (deferred): una línea fallida deja una entrada huérfana en `payload_SAP` sin contraparte en `response_SAP`, así que los dos arrays dejan de corresponder por índice en corridas parciales. Nadie los lee posicionalmente hoy. ← PUNTERO A T8: trampa de diagnóstico al leer un audit de fallo parcial.
Task 6: minor (deferred): sólo se cachean los éxitos, no los fallos. Cinco líneas del mismo producto roto cuestan cinco idas a SAP y cinco entradas en `roundFailures`.
Task 6: minor (deferred): `meta.skippedCount` mezcla los fallos `hubspot_read` de T4 con los `sap_price` de T6 sin desglose (brief-mandated). El detalle de `stage` sobrevive en `roundFailures` para T8.
Task 6: minor (deferred): ← PUNTERO A T8, IMPORTA — las formas de `roundFailures` difieren según el stage: las de `hubspot_read` traen `endpoint` y NO traen `itemCode`; las de `sap_price` traen `itemCode` y NO traen `endpoint`. T8 tiene que tolerar ambas.
Task 6: minor (deferred): el `catch` del ciclo no filtra: cualquier throw de `resolveSapPricing`, incluido un `TypeError` de un bug real o un throw no-Error (donde `error.message` es undefined), queda etiquetado `stage: 'sap_price'`.
Task 6: nota del implementador para T7/T8 — `sapCache`, `roundFailures`, `auditRounds` y `callRecorder` viven dentro del `try` de `execute`; T8 los necesita fuera para que el `catch` los vea, como el plan ya prescribe.
Task 7: implementado en c6834c6..2b51c51, reportado DONE_WITH_CONCERNS.
Task 7: concern 1 CONFIRMADO por el controller y es real — `await this.reconcile(...)` en SyncLineItemPrices.js:570 está sin proteger y sale ANTES de la escritura del amount (:587). Si la ronda 1 escribe precios y después falla la relectura del deal, `execute` se cae y el deal queda con precios nuevos y total viejo. Lo introdujo mi plan al mover el amount al cierre; antes se escribía justo después de la ronda 1. No se auto-cura hoy porque los reintentos de HubSpot se descartan como duplicados hasta que aterrice T9.
Task 7: DECISIÓN DEL USUARIO (2026-08-17): proteger la reconciliación con try/catch. Una pasada de seguridad que falla no debe tirar una corrida que ya escribió precios bien; se anota en el audit y el amount se escribe con lo de la ronda 1.
Task 7: nota para T8 — el fix agrega un TERCER shape de `failures`: `stage: 'reconciliation'`, sin `id` ni `itemCode`, de pasada completa y no por línea. T8 tiene que tolerar los tres (`hubspot_read`, `sap_price`, `reconciliation`).
Task 7: el implementador encontró y arregló un bug del helper de tests: `createUseCase` devolvía los dobles por defecto en vez de los que recibió el use case, así que el test 3 del brief aseguraba sobre un mock que nadie llamó. Fix legítimo, se conserva.
Task 7: fix round 1/5 (a-g todas addressed, 0 open — try/catch dentro del guard de dealId, warn con tenantKey/dealId/message, resultado del mismo shape + `aborted:true` + failure `stage:'reconciliation'`, `aborted` en meta, amount escrito con la ronda 1, `readDealLineItemIds` intacto, test que asegura las cuatro cosas con valores concretos; commits 2b51c51..248fc55)
Task 7: DESVIACIÓN ACEPTADA por el controller — el implementador expuso `failures: reconciliation.failures` en `meta.reconciliation`, más allá del `aborted` que pedí. Razón: era la única costura observable para asegurar `stage:'reconciliation'` (la lista es local de `execute`, `meta` sólo llevaba contadores). Rechazó bien las alternativas (el logger no puede fijar `stage`; empujar a `roundFailures` corrompería `skippedCount`). Beneficio lateral: los fallos de reconciliación se vuelven visibles en la respuesta HTTP, que importa porque hoy no llegan a ningún humano hasta T8.
Task 7: minor (deferred): el `catch` es amplio — envuelve todo `reconcile()`, no sólo la relectura, así que un fallo del `updateLineItems` de la ronda 2 o un bug genuino adentro quedan con el mensaje fijo "deal line items could not be re-read", que es inexacto para esos casos. Es spec-compliant (lo pidió mi parte (a)); vale ajustar la redacción del log.
Task 7: complete (commits c6834c6..248fc55, review clean)
Task 7: notas para T8 (del re-reviewer, out of scope allí): `reconciliation.enriched` alimenta sólo `totalAmount`, no `data.lineItems` (sigue siendo de la ronda 1); la ronda 2 no llama a `updateProducts`; y `meta.skippedCount` sigue contando sólo los `roundFailures` de la ronda 1, no los de la reconciliación.
Task 8: implementado en 248fc55..02101fa (8 archivos, +438/-17), reportado DONE_WITH_CONCERNS. Cableado verificado por el controller de punta a punta: composition (import + clave), constructor + asignación, ambos call sites del use case (:678 éxito, :716 catch), y el camino fatal del servicio (:634).
Task 8: review 1 (Opus) — spec ✅ con un ⚠️, quality **Needs fixes**. 2 Important, ambos plan-mandated (defectos de MI plan), 4 Minor.
Task 8: Important 1 — el audit fatal hardcodea `unresolved: []` (SyncLineItemPrices.js:721). En cualquier throw antes del push de la ronda 1 (:545) — validatePayload, credenciales, o `fetchActiveDiscountGroups` en una caída de SAP — el documento queda con `rounds: []` y `unresolved: []`, así que los fallos `hubspot_read` que el lector tolerante ya había puesto en `payload.lineItemFailures` desaparecen. Es exactamente la evidencia del 404 que el plan existe para capturar. El remedio que propuso el implementador (concern 3) NO lo arreglaba: el `catch` nunca lee `roundFailures`, pasa un `[]` literal. Hacen falta las dos mitades.
Task 8: Important 2 — `unresolved` (:683-686) lista líneas que la ronda 2 SÍ valorizó, porque `reconcile` reintenta las que fallaron en la ronda 1 y `resolveSapPricing` sólo memoiza éxitos, así que el reintento realmente vuelve a SAP y puede salir bien; la entrada de fallo original queda igual. Además duplica la línea que falla en las dos rondas.
Task 8: DECISIONES DEL USUARIO (2026-08-17): (1) arreglar la pérdida de evidencia; (2) FILTRAR lo que la ronda 2 resolvió, no renombrar el campo. `unresolved` debe significar lo que su nombre dice.
Task 8: el controller incluyó además 2 Minor en la misma ronda por estar atados al fix: el comentario del hoisting (:386-388) que afirma que el `catch` usa `roundFailures`/`reconciliation` cuando no los lee — es justo lo que convencería a un lector futuro de que el Important 1 no puede pasar — y el rename de `lineItemIdsFromDeal` a `lineItemIdsInPayload` (:547), porque mapea las líneas que SÍ se pudieron leer y alguien diagnosticando lo leería como "lo que el deal tenía".
Task 8: minor (deferred): `execute` quedó en ~345 líneas con el ensamblado del audit disperso en 4 puntos (:545, :623, :678, :716). Extraer un `buildAudit(...)` privado volvería los Important 1 y 2 arreglos de un solo sitio. Refactor real, no en esta ronda.
Task 8: minor (deferred): el audit fatal de `preparePayload` va con `calls: []`, así que la secuencia de GETs previa queda invisible en ese camino. Endpoint y status sí se capturan, que era el requisito.
Task 8: minor (deferred, ABIERTO PARA LA RELEASE): `meta.skippedCount` y los contadores del syncLog del controlador NO se cambiaron (fuera del brief). Un éxito parcial sigue devolviendo `ok: true` con contadores que ignoran las líneas descartadas. La visibilidad que aterriza en T8 es el documento `audit` persistido, no los contadores ni la respuesta HTTP. Si la release también necesita los contadores, sigue abierto.
Task 8: verificado por el reviewer — el audit NO viaja en la respuesta HTTP (el controlador responde `{ok, data, meta}`), no hay credenciales en el audit (el `data` grabado de SAP es el payload de precio, no `sapConfig`), y los cuerpos de request sólo se adjuntan en llamadas fallidas, sanitizados y truncados, con tope de 200 llamadas.
Task 8: fix round 1/5 (Important 1, Important 2, Minor A y Minor B addressed, 0 open; commits 02101fa..558497c). `collectUnresolvedFailures` filtra por los ids de `reconciliation.priced`, deduplica por id, y deja intactas las entradas SIN id (la de `stage:'reconciliation'`). El test exhaustivo de los tres shapes quedó sin tocar y sigue detectando una normalización.
Task 8: el implementador rebatió con razón la parte condicional del Minor A: mantuvo `reconciliation` hoisted porque tras el fix del Important 2 el `catch` SÍ lo lee (un throw en el PATCH del amount ocurre después de la reconciliación y sí llega al catch externo; el `reconcile` en sí se atrapa localmente). Verificado por el re-reviewer contra el código.
Task 8: minor NUEVO (deferred): `reconciliation.priced` se usa como señal de "esta línea se escribió", pero la ronda 2 descarta la respuesta de `updateLineItems` sin mirar sus `results`, mientras la ronda 1 sí usa la señal fuerte (`updatedIds` desde `response.results`). Si el batch de la ronda 2 se acepta pero reporta un error por input, la línea queda filtrada de `unresolved` y el audit sub-reporta una línea que quedó sin precio. Sólo aplica al caso de batch parcial.
Task 8: minor NUEVO (deferred): el dedupe "gana la más temprana" es el criterio equivocado cuando las dos entradas traen razones distintas. Un `sap_price` de la ronda 1 más un `hubspot_read` (404) de la ronda 2 para la misma línea deja visible sólo la razón vieja; la más accionable y reciente se pierde de `unresolved`. Las dos sobreviven en `rounds[].failures`, así que no se pierde información, y el test de "falla en las dos rondas" no distingue el caso porque usa razones idénticas.
Task 8: complete (commits 248fc55..558497c, review clean)
Task 9: implementado en 558497c..1954ca7. review 1 (Opus) — spec ❌, quality **Needs fixes**: 1 CRITICAL, 2 Important, 4 Minor. Todos plan-mandated (código de MI brief).
Task 9: CRITICAL, verificado por el controller en el código — el fallback de `E11000` (lineItemPriceWebhook.service.js:616-618) re-consulta con el `duplicateFilter` pelado, sin condición de vida, así que puede adoptar un registro EN VUELO o ya EXITOSO y cae al procesamiento. Traza sin fallo previo: HubSpot entrega el mismo evento dos veces; A inserta y trabaja; B choca con el índice único, entra al fallback, encuentra la fila en vuelo de A, la adopta y valoriza el deal por segunda vez, escribiendo `markAsSent`/`markAsError` contra el `_id` de A. Antes de este diff esa rama devolvía `duplicate_event` y los dos casos se descartaban bien: es una REGRESIÓN que introduce este trabajo, con escrituras duplicadas de precios sobre deals reales.
Task 9: Important — el camino de reutilización (:597-605) es read-then-write sin compare-and-swap: dos reintentos simultáneos leen el mismo registro fallido, los dos lo resetean, los dos procesan. Arreglar el Critical no cierra esta carrera, es otra.
Task 9: Important — la rama entera de `E11000` no se ejecuta en NINGÚN test (el `create` del doble nunca rechaza), que es justo donde vivía el Critical. Y los dos tests nuevos no distinguen un reenvío EXITOSO (`isSend:true`) de uno EN VUELO (`errorMessage:null`) porque el `findOne` del doble ignora su filtro.
Task 9: DECISIÓN DEL USUARIO (2026-08-17): cerrar las dos carreras — condición de vida en el fallback + claim atómico con `findOneAndUpdate`. El controller LEVANTÓ su propia restricción de "no tocar el doble `buildTenantModels`": se permite AGREGARLE `findOneAndUpdate`, sin cambiar el comportamiento de `findOne`/`create`/`updateOne` de los que dependen otros tests.
Task 9: DESCUBRIMIENTO que corrige una premisa del spec — la estrategia hermana `dealPriceList` que cité como modelo está ELLA MISMA sin arreglar: tiene el `$or` en `findDuplicate` (:206-209) pero llama a `create` sin ningún manejo de `E11000` (:152), así que un reintento ahí pasa el guard y muere en el índice único. Lo que el spec afirma ("la hermana ya se protege") es verdad a medias. Suma a la deuda de migrar `dealPriceList`.
Task 9: minor (deferred): el reset no limpia `audit`. Alcanzable: si un reintento sale bien pero su audit serializa a `null`, `persistAudit` lo saltea y el documento queda `isSend:true` junto al audit del intento fallido anterior. NO se arregla poniendo `audit: null` en el reset — eso destruiría el único registro de por qué falló el intento 1. Necesita diseño (escritura incondicional del audit en las transiciones terminales, o estampar el intento).
Task 9: minor (deferred): si el proceso muere después del claim sin llegar a `markAsError`, la fila queda permanentemente "en vuelo" y todo reintento futuro se descarta en el guard — el mismo "transitorio se vuelve permanente" que esta tarea arregla, por otra ruta. Inherente a la codificación del spec y compartido con la estrategia hermana. Amerita un barrido de eventos trabados a nivel de rama.
Task 9: fix round 1/5 (Critical + los 2 Important addressed, 0 open; commits 1954ca7..6407f2c). Un solo helper `claimFailedAttempt` (:500) con `findOneAndUpdate` atómico y filtro `{isSend:false, errorMessage:{$ne:null}}`, usado en los dos sitios (:622, :636). Cero `findOne(duplicateFilter)` pelados (verificado por el controller con grep -c = 0).
Task 9: DESVIACIÓN ACEPTADA — el implementador usó el claim atómico también en el fallback de E11000, en vez del `findOne` literal que yo especifiqué. Su razón, confirmada por el re-reviewer contra el código: un `findOne` ahí adoptaría el registro SIN limpiar `errorMessage`, dejándolo reclamable durante todo el procesamiento, lo que reabriría el Important en ese segundo sitio. Es estrictamente mejor que lo que yo pedí.
Task 9: el re-reviewer verificó que los dos filtros son complementos EXACTOS sobre el schema real (ambos campos tienen default, así que ningún registro cae entre los dos) y recorrió las dos trazas: dos entregas simultáneas y dos reintentos simultáneos terminan en `skip: duplicate_event`, y el controlador corta en `skip` antes de asignar `executionId`, así que ni `markAsSent` ni `markAsError` pueden aterrizar sobre el documento del ganador. La mitad de "escrituras interleaved" del Critical queda cerrada estructuralmente, no sólo por filtro.
Task 9: minor (deferred): la aserción del filtro del claim usa `expect.objectContaining` con sólo las dos llaves de estado, así que borrar el spread `...duplicateFilter` dejaría al claim adoptar el evento fallido de CUALQUIER tenant y todos los tests seguirían verdes. El código está bien hoy; es hueco de cobertura.
Task 9: minor (deferred): el test `still skips a resend of an event that is currently in flight` documenta el invariante pero no lo verifica — `stubStoredEvent` clasifica con la misma expresión que el código bajo prueba. El par de tests sí es adecuado porque el hermano asegura el `$or` explícito.
Task 9: complete (commits 558497c..6407f2c, review clean)
REVISIÓN FINAL DE RAMA (Opus, dd9b9a7..6407f2c, 16 commits): "Ready to merge: With fixes". 1 Critical, 5 Important, 6 Minor, más triaje de los ~20 minors diferidos.
FINAL Critical — la ronda 2 re-implementa el enriquecimiento de la ronda 1 y las dos copias divergieron, así que la pasada de SEGURIDAD puede escribir un precio EQUIVOCADO sobre uno correcto: (a) `reconcile` lee con `extraProperties: ['price']` y nunca pide el `miscSourceProperty`, así que el uplift de misc sale null y reescribe el precio crudo, y además sobreescribe `safe_price_value`; (b) no recibe `discountConfig`/`activeDiscountGroups` y hardcodea `Discount: 0` mientras sigue pasando `_discountHsProperty`, así que escribe `discount:"0"` sobre lo que la ronda 1 resolvió; (c) descarta el `priceCalculation.warning`. Disparador nada exótico: la rama `zero_price` existe para reescribir líneas, y `count_mismatch` se activa en todo deal con un SKU sin precio en SAP. Ningún test lo veía porque los de reconciliación usan tenant sin misc ni descuentos. ES UN ERROR DE MI PLAN (dispatché la ronda 2 diciendo que armara sus propias líneas "intencionalmente").
FINAL Important 2 — el grabador sigue en `DEFAULT_MAX_CALLS = 40` porque `createSapCallRecorder()` se llama sin opciones, mientras el serializador dice 200. Un deal de ~13 líneas llena el recorder antes de la fase de escritura, así que las llamadas más propensas a fallar (batch writes, searches rate-limited, PATCH del deal) nunca llegan al audit. Y `droppedCalls` se calcula sobre el array ya truncado, así que el audit afirma que no se perdió nada.
FINAL Important 3 — cuando TODAS las líneas dan 404, `buildLegacyPayload` lanza y descarta `failures`, así que el audit queda con `fatalError` de status/endpoint null: el mismo punto ciego del reporte original, una capa más arriba. Es justo el escenario de la hipótesis del negocio en un deal de una línea.
FINAL Important 4 — la relectura de la reconciliación es incondicional (~2N+6 llamadas a HubSpot en un deal de 20 líneas). DECISIÓN DEL USUARIO (2026-08-18): dejarla SIEMPRE, como se definió; es lo que detecta una línea que apareció después de la primera lectura.
FINAL Important 5 — `execute` en ~360 líneas con el audit ensamblado en 4 puntos; es la causa raíz del Critical. El fix del Critical debe tomar la forma de la extracción, no de un parche.
FINAL triaje de diferidos: contadores del syncLog → tomar ahora (2 expresiones); audit rancio tras reintento → deuda con mitigación barata sugerida en `persistAudit`; `dealPriceList` sin manejo de E11000 → deuda pero es un BUG VIVO, no sólo deuda de arquitectura, y corrige la premisa del spec; resto → deuda.
FIX WAVE FINAL despachada: Critical 1 (extracción compartida + misc + descuentos + warning), Important 2 (maxCalls 200 + droppedCalls real), Important 3 (failures al audit fatal), contadores del syncLog, y Minor 7 (`dealId` en el evento de asociación — es POR QUÉ el documento del reporte decía `dealId: null` y vuelve inútil el índice {dealId, createdAt} justo para los eventos que se diagnostican).
FIX WAVE FINAL completada (commits 6407f2c..696c7cc): 1f45471 extracción compartida `buildEnrichedLineItem`, b5181cc maxCalls 200 + droppedCalls real, 66c5d9c failures al audit fatal, f1a21f8 contadores del syncLog, 696c7cc dealId en el evento de asociación. 8 tests nuevos.
RE-REVIEW de la tanda (Opus): los 5 fixes ADDRESSED y las 3 afirmaciones del reporte verificadas — incluida la más importante, que ninguna aserción preexistente de precios de la ronda 1 cambió (el único `-` en ese archivo de test es una línea `await` convertida en `const result = await`). Verificó campo por campo que `buildEnrichedLineItem` produce salida idéntica a la del bloque inline borrado.
RE-REVIEW — 1 Important NUEVO introducido por el fix wave: guardar `dealId` hizo que los eventos de asociación entren en la ventana de debounce del cambio de `misc` (handlePropertyChangeEventSkipped), donde antes no podían porque `dealId` era null. Un `misc` que llegue dentro de la ventana se descarta como `debounced_event`.
RE-REVIEW — CORRECCIÓN del controller a la severidad reportada: el reviewer afirmó que el debounce está activo por defecto. NO lo está. El debounce vive en `handlePropertyChangeEventSkipped` (:297) y sólo se llega ahí si el tenant configura `skippedInWebhooksInPropertyChange = 'SkippedVersion'` (:585); el default es `Legacy` (:30), que no tiene debounce. El reviewer confundió el default de la CONFIG del debounce (activada) con el del MODO. Además la hermana `dealPriceList` ya guardaba `dealId` en eventos de asociación y ya debounceaba contra él, así que el comportamiento existe en producción desde antes: FIX 5 alineó `businessPartner` con la hermana, no inventó el peligro.
DECISIÓN DEL USUARIO (2026-08-18): acotar el debounce a eventos de propertyChange. Cerrado en c84c635 con restricción POSITIVA `'payload.subscriptionType': SUPPORTED_SUBSCRIPTION_TYPE` (no "distinto de association") para que un tercer tipo de evento no reabra el hueco, más comentario del por qué y test de las dos direcciones. La hermana `dealPriceList` NO se tocó (fuera de alcance, verificado con git diff --stat vacío).
ESTADO FINAL: suite 4 failed suites / 7 failed tests = baseline exacto de antes de la rama (excluyendo la flaky `internalTenant`), 1340 passing / 1351 total. 17 commits desde dd9b9a7. Cero regresiones en toda la ejecución, verificado por el controller después de cada tarea.
Task 4: nota para T9 — en un deal de UNA sola línea, el escenario disparador (el asesor borra la única línea, el índice va atrasado) da cero líneas legibles → 500 → HubSpot reintenta → ya sin asociaciones → también fatal. Ese evento nunca puede llegar a éxito. Es lo que el spec pide, pero interactúa con el manejo de reintentos de T9.
FIX WAVE FINAL: completa (commits 6407f2c..696c7cc, 5 commits, uno por fix). Reporte en `.superpowers/sdd/2026-08-17-line-item-price-partial-failure-audit/final-fix-report.md`.
FIX WAVE FINAL — Critical 1 cerrado por EXTRACCIÓN: `buildEnrichedLineItem` es ahora la única fuente del enriquecimiento y la usan las DOS rondas; `reconcile` recibe un solo objeto `enrichment` (el mismo que arma la ronda 1) en vez de 4 params sueltos, y lee con `[miscSourceProperty, 'price']`. Ninguna aserción de pricing de la ronda 1 cambió, y los 2 tests deliberados de `resolveTaxRate` siguen rojos por la MISMA causa (`Discount: 15` vs `0`).
FIX WAVE FINAL — Important 2 cerrado: `createSapCallRecorder({ maxCalls: 200 })` y el `droppedCalls` del grabador se suma al que calcula el serializador. El noop tolera el argumento (verificado con aserción, no supuesto). Test nuevo que recorre la costura con el grabador REAL (205 llamadas → 200 guardadas / 5 descartadas), que es justo lo que el test sintético del builder tapaba.
FIX WAVE FINAL — Important 3 cerrado: `error.lineItemFailures` viaja pegado al fatal `Deal has no readable line items` y entra al audit como `unresolved`; el test fija endpoint Y status del 404.
FIX WAVE FINAL — contadores del syncLog cerrados: `recordsProcessed`/`failed` suman `meta.skippedCount`. Minor 7 cerrado: `dealId` en el `create` de la rama de asociación y en el `$set` del claim.
FIX WAVE FINAL: 3 aserciones preexistentes se ENDURECIERON (no se debilitaron), porque los fixes agregan campos a objetos que ya se aseguraban por igualdad exacta: el `create` del evento de asociación (x2, ahora con `dealId`) y el `$set` del claim.
FIX WAVE FINAL: nada de la lista de deuda aceptada se tocó — la relectura sigue incondicional, sin más refactor de `execute`, `dealPriceList` intacto, y audit rancio / stuck-in-flight / `truncateAuditBody` / resultados del batch de la ronda 2 / dedupe "gana la más temprana" / entrada redundante del `batch/update` siguen abiertos como deuda.
FIX WAVE FINAL: suite completa 5 suites / 11 tests rojos = los 4 preexistentes + la flaky `internalTenant` (verde 4/4 sola). Excluyéndola: 4 suites / 7 tests, 1339 passing / 1350 total (baseline 1331 / 1342, +8 tests nuevos todos verdes).
